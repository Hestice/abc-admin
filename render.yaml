services:
  - type: web
    name: api
    env: node
    plan: free
    region: singapore
    buildCommand: |
      NPM_CONFIG_PRODUCTION=false npm install
      npx nx build api
    startCommand: node apps/api/dist/main.js
