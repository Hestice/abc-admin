services:
  - type: web
    name: api
    env: node
    plan: free
    region: singapore
    buildCommand: |
      NPM_CONFIG_PRODUCTION=false npm install
      cd apps/api && NODE_ENV=production npx --yes webpack-cli build --config webpack.config.js --node-env=production
    startCommand: node apps/api/dist/main.js
