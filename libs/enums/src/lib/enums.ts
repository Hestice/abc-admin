export enum UserRole {
  ADMIN = 'admin',
}

export enum Sex {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum Category {
  ONE = 1,
  TWO = 2,
  THREE = 3,
}

export enum Status {
  ALIVE = 'alive',
  DEAD = 'dead',
  UNKNOWN = 'unknown',
  LOST = 'lost',
}
